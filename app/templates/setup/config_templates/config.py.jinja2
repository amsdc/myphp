"""MyPHP Configuration File

Generated by Web Installer.
"""

import os
from dotenv import load_dotenv

basedir = os.path.abspath(os.path.dirname(__file__))
load_dotenv(os.path.join(basedir, '.env'))

class Config(object):
    # Application secret key. Keep this secret!
    SECRET_KEY = "{{ secret_key }}"

    # Database Configuration
    SQLALCHEMY_DATABASE_URI = "{{ database_uri }}"

    # Leave as is.
    SQLALCHEMY_TRACK_MODIFICATIONS = False

    # Captcha Site Key
    XCAPTCHA_SITE_KEY = "{{ captcha_site_key }}"

    # Captcha Secret Key
    XCAPTCHA_SECRET_KEY = "{{ captcha_secret_key }}"

    {% if hcaptcha %}
    # This enables HCaptcha. Remove these 3 lines to use
    # reCaptcha.
    XCAPTCHA_VERIFY_URL="https://hcaptcha.com/siteverify"
    XCAPTCHA_API_URL="https://hcaptcha.com/1/api.js"
    XCAPTCHA_DIV_CLASS="h-captcha"
    {% else %}
    # This enables HCaptcha. Uncomment these 3 lines to use
    # HCaptcha.
    # XCAPTCHA_VERIFY_URL="https://hcaptcha.com/siteverify"
    # XCAPTCHA_API_URL="https://hcaptcha.com/1/api.js"
    # XCAPTCHA_DIV_CLASS="h-captcha"
    {% endif %}

    # The below configuration values should not be modified.
    # Leave them as is.

    # These are the names of the cookies.
    # These are not Britannia or Parle cookies, they are web cookies.
    # It is safer not to mess around with these.
    # P.S. Custom cookie names are mainly for fancy purposes :)
    SESSION_COOKIE_NAME = "myphp_session"
    REMEMBER_COOKIE_NAME = "myphp_remember_token"

    # These are for server-side sessions.
    SESSION_TYPE = "filesystem"
    SESSION_FILE_DIR = ".session-data/"

    # This is for password reset emails.
    MAIL_SERVER = os.environ.get('MAIL_SERVER')
    MAIL_PORT = int(os.environ.get('MAIL_PORT') or 25)
    MAIL_USE_TLS = os.environ.get('MAIL_USE_TLS') is not None
    MAIL_USERNAME = os.environ.get('MAIL_USERNAME')
    MAIL_PASSWORD = os.environ.get('MAIL_PASSWORD')
    ADMINS = ['your-email@example.com']

    # This decides whether to launch Setup or not.
    MYPHP_SETUP = True if str(os.environ.get('MYPHP_SETUP')).lower() == \
        'true' else False